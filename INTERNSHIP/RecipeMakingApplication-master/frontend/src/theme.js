// src/theme.js
import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    brand: {
      900: "#1A365D",
      800: "#153E75",
      700: "#2A69AC",
    },
  },
});

export default theme;
